<UserControl x:Class="CommunicationProtocol.WpfApp.ModbusControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CommunicationProtocol.WpfApp" xmlns:c="clr-namespace:CalcBinding;assembly=CalcBinding"
             mc:Ignorable="d" 
             Loaded="UserControl_Loaded"
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             d:DesignHeight="1000" d:DesignWidth="1200">
    <Grid>
        <GroupBox Header="Modbus" Margin="5" FontSize="20">
            <Grid>
                <Grid.Resources>
                    <Style TargetType="GroupBox" BasedOn="{StaticResource MaterialDesignGroupBox}">
                        <Setter Property="Margin" Value="5,0,5,0"/>
                    </Style>
                </Grid.Resources>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <local:SerialPortSettingControl SelectedSettings="{Binding SelectedSettings}" 
                                          CollectionSettings="{Binding CollectionSettings}"
                                          DataContext="{Binding DataContext}"/>
                <GroupBox Header="Data" Grid.Column="1" >
                    <ListBox ItemsSource="{Binding InputSignals}"  SelectedIndex="{Binding SelectedInputIndex}" AlternationCount="{Binding InputSignals.Length}" 
                         ScrollViewer.VerticalScrollBarVisibility="Disabled"  HorizontalAlignment="Center">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Vertical"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem" BasedOn="{StaticResource MaterialDesignListBoxItem}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Style.Triggers>
                                    <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                        <Setter Property="IsSelected" Value="True" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <WrapPanel Margin="0,0,10,0">
                                    <WrapPanel>
                                        <TextBlock Text="{Binding Path=(ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Foreground="Black" Padding="0,15,0,0" Width="30"/>
                                        <TextBox Background="LightGoldenrodYellow"  TextAlignment="Center"  Text="{Binding Path=.}" Padding="0,15,0,0" Width="50"/>
                                    </WrapPanel>
                                </WrapPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </GroupBox>
                <UniformGrid Rows="2" Grid.Column="2">
                    <UniformGrid.Resources>
                        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                            <Setter Property="Width" Value="120"/>
                            <Setter Property="Height" Value="50"/>
                        </Style>
                    </UniformGrid.Resources>
                    <StackPanel VerticalAlignment="Top">
                        <Button Content="Connect" Click="Connect" IsEnabled="{c:Binding !IsEnabled,ElementName=disconnectButton, FalseToVisibility=Collapsed}"/>
                        <Button Content="Disconnect" Click="Disconnect" x:Name="disconnectButton" Margin="0,15,0,0" IsEnabled="{Binding ModbusController.IsConnected}"/>
                    </StackPanel>
                    <StackPanel VerticalAlignment="Bottom">
                        <Button  VerticalAlignment="Bottom" Margin="0,0,0,15" Content="Clear" Click="ClearMessage"/>
                        <Button  Margin="10,0"  Content="Send" Click="SendMessage" IsEnabled="{Binding ModbusController.IsConnected}"/>
                    </StackPanel>
                </UniformGrid>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
